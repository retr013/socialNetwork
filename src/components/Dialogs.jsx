import React from 'react';
import style from '../styles/Dialogs.module.css'

const Dialogs = (props) => {
    return (
        <div>
            {props.a}
        </div>
    );
};

export default Dialogs;
